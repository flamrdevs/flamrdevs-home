import { style } from "@vanilla-extract/css";

const Root = style({
	display: "flex",
	flexDirection: "column",
	justifyContent: "center",
	alignItems: "stretch",
	gap: "0.5rem",
});

export { Root };
