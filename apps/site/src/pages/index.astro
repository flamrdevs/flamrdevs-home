---
import { GithubIcon, MoonIcon, SunIcon } from "lucide-solid";

import { LINK } from "@flamrdevs/const/exports";

import { Center, Container, HTMLX, IconButton, IconButtonLink, Separator } from "~/components/core";
import { AnimatedSVGLogo } from "~/components/flamrdevs";

import { flamrdevssvg } from "~/styles/pages/index.css";
---

<HTMLX>
	<Container max="lg" p="2">
		<Separator orientation="horizontal" />
	</Container>

	<Center>
		<AnimatedSVGLogo class={flamrdevssvg} />
	</Center>

	<Container max="lg" p="2">
		<Separator orientation="horizontal" />
	</Container>

	<Container max="lg" p="2">
		<Center gap="2">
			<IconButtonLink href={LINK.GITHUB} target="_blank">
				<GithubIcon client:idle />
			</IconButtonLink>
		</Center>
	</Container>

	<Container max="lg" p="2">
		<Center gap="2">
			<IconButton id="light-theme-icon-button">
				<SunIcon client:idle />
			</IconButton>

			<IconButton id="dark-theme-icon-button">
				<MoonIcon client:idle />
			</IconButton>
		</Center>
	</Container>
</HTMLX>

<script>
	import { changeTheme } from "~/stores/theme";

	document.getElementById("light-theme-icon-button")!.addEventListener("click", () => {
		changeTheme("light");
	});
	document.getElementById("dark-theme-icon-button")!.addEventListener("click", () => {
		changeTheme("dark");
	});
</script>
